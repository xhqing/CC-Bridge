# KIMI-BRIDGE — Claude Code ↔ Kimi（月之暗面 Moonshot）配置模板（预留）
# 运行配置落地路径：~/.cc-bridge/kimi.env（本模板首次使用时由框架自动复制到此处）
# ============================================================================
# ⚠️ 未实现：此上游在 core/adapter.js 注册表里 implemented: false，运行
#    `cc-bridge kimi …` 会报「未实现」。下方字段为预留示例，等
#    kimi-bridge/adapter.js 落地后才会真正生效。实现步骤见
#    kimi-bridge/README.md。
#
# 字段含义与 glm-bridge/glm.env.example 一致（框架字段统一），仅上游专属值不同：
#   API_BASE     : Moonshot 接口地址（Anthropic 兼容端点）。
#   API_KEY_n    : Moonshot API KEY，每个写成独立编号变量 API_KEY_1、API_KEY_2 …（多 KEY
#                  容灾，推荐至少 2 个）。每个 KEY 独立成行，可单独注释账号来源或整行
#                  注释掉禁用某 KEY。旧式 API_KEY=k1,k2（逗号分隔）仍向后兼容。
#                  详细写法见 glm-bridge/glm.env.example。
#   API_KEY_n_HIDE_USER_ID : （可选，隐私选项）配 1 时该 KEY 转发的请求会清空
#                  metadata.user_id（Claude Code 自动附带的设备 / 会话标识）；不配或配 0
#                  则原样透传。按 KEY 独立设置。
#   MODEL_MAP    : 模型映射「spoofA->targetA,spoofB->targetB」。左侧是 Claude 白名单
#                  模型 ID（Claude Code 只接受白名单内的模型 ID），右侧是 Moonshot 真实
#                  模型名；桥在转发前把 body.model 从左侧改写成右侧，上游永远看不到 spoof 值。
#                  支持多对：opus 做主力、haiku 做轻量任务，两对都指向 kimi-k2。第一对
#                  是主力对（claude 启动默认模型）。旧式 SPOOF_MODEL / TARGET_MODEL 单对
#                  写法仍向后兼容（等价于一对）。
#   PROXY_PORT   : 本地监听端口（多上游并存时各用不同端口）。
#   PROXY_LOG    : 1 = 记录每条请求（默认），0 = 静默。
#
# 注：下列数值为该厂商公开默认值的占位，仅供参考、不保证可用——以你实际开通的
#     模型 / 套餐为准，并在实现 adapter 时核对（模型名、上下文窗口、max_tokens 上限、
#     effort 档位映射等）。思考字段不做桥接改写、/effort 档位原样透传。
# ============================================================================

# --- Kimi 上游（月之暗面 Moonshot）---
API_BASE=https://api.moonshot.cn
# 账号 A 的 KEY
API_KEY_1=your_moonshot_key_1
# 账号 B 的 KEY
API_KEY_2=your_moonshot_key_2
# 旧式写法 API_KEY=k1,k2（逗号分隔）仍向后兼容。

# 模型映射（多对，预留示例）：两对都指向 kimi-k2。第一对 opus 为主力对。
MODEL_MAP=claude-opus-4-8->kimi-k2,claude-haiku-4-5->kimi-k2

PROXY_PORT=8789
PROXY_LOG=1
