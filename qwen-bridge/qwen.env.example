# QWEN-BRIDGE — Claude Code ↔ Qwen（阿里云百炼 DashScope）配置模板（预留）
# 运行配置落地路径：~/.cc-bridge/qwen.env（本模板首次使用时由框架自动复制到此处）
# ============================================================================
# ⚠️ 未实现：此上游在 core/adapter.js 注册表里 implemented: false，运行
#    `cc-bridge qwen …` 会报「未实现」。下方字段为预留示例，等
#    qwen-bridge/adapter.js 落地后才会真正生效。实现步骤见
#    qwen-bridge/README.md。
#
# 字段含义与 glm-bridge/glm.env.example 一致（框架字段统一），仅上游专属值不同：
#   API_BASE     : DashScope 接口地址（Anthropic 兼容端点）。
#   API_KEY_n    : DashScope API KEY，每个写成独立编号变量 API_KEY_1、API_KEY_2 …（多 KEY
#                  容灾，推荐至少 2 个）。每个 KEY 独立成行，可单独注释账号来源或整行
#                  注释掉禁用某 KEY。旧式 API_KEY=k1,k2（逗号分隔）仍向后兼容。
#                  详细写法见 glm-bridge/glm.env.example。
#   MODEL_MAP    : 模型映射「spoofA->targetA,spoofB->targetB」。左侧是 Claude 白名单
#                  模型 ID（绕过客户端 effort 闸门），右侧是 DashScope 真实模型名；桥
#                  在转发前把 body.model 从左侧改写成右侧，上游永远看不到 spoof 值。
#                  支持多对：opus 做主力、haiku 做轻量任务，两对都指向 qwen3-coder-plus。
#                  第一对是主力对（claude 启动默认模型）。旧式 SPOOF_MODEL /
#                  TARGET_MODEL 单对写法仍向后兼容（等价于一对）。
#   PROXY_PORT   : 本地监听端口（多上游并存时各用不同端口）。
#   PROXY_LOG    : 1 = 记录每条请求（默认），0 = 静默。
#
# 注：下列数值为该厂商公开默认值的占位，仅供参考、不保证可用——以你实际开通的
#     模型 / 套餐为准，并在实现 adapter 时核对（模型名、上下文窗口、max_tokens 上限、
#     是否需要强制 max effort 等）。
# ============================================================================

# --- Qwen 上游（阿里云百炼 DashScope）---
API_BASE=https://dashscope.aliyuncs.com
# 账号 A 的 KEY
API_KEY_1=your_dashscope_key_1
# 账号 B 的 KEY
API_KEY_2=your_dashscope_key_2
# 旧式写法 API_KEY=k1,k2（逗号分隔）仍向后兼容。

# 模型映射（多对，预留示例）：两对都指向 qwen3-coder-plus。第一对 opus 为主力对。
MODEL_MAP=claude-opus-4-8->qwen3-coder-plus,claude-haiku-4-5->qwen3-coder-plus

PROXY_PORT=8790
PROXY_LOG=1
